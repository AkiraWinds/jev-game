# jev-game

A minimal Python starter project.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Run

```bash
python -m jev_game.main
```

## Test

```bash
pip install pytest
pytest
```
